import React from "react";
import { StyleSheet, Text, View } from "react-native";

const NewPlace = () => {
  return (
    <View>
      <Text> New Places </Text>
    </View>
  );
};

export default NewPlace;

const styles = StyleSheet.create({});
